apiVersion: v1
kind: Service
metadata:
  name: web-service
spec:
  selector:
    app: synergychat-web
  ports:
    - protocol: TCP
      port: 80 # port that the server will listen on
      targetPort: 8080 # port that the pods are listening on

